edge cases -> saving versions

- what if its the same code that was saved?
- what if user presses save button fast but debounce is not yet executed


questions
- why does the body only converted to json but not the method and header?